import React from 'react'
import { motion } from 'framer-motion'

const PersonSpace = () => {
  return (
    <motion.div
      style={{ height: '100vh' }}
      initial={false}
    >
      PersonSpace
    </motion.div>
  )
}

export default PersonSpace
